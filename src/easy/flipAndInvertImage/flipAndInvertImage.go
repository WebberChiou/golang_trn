package flipAndInvertImage

func FlipAndInvertImage(A [][]int) [][]int {
	/*
		if(A == null || A.length == 0 || A[0].length == 0){
				return A;
			}
			int len = A[0].length;

			for(int[] b : A){
				for(int i = 0; i < len / 2; i++){
					int tmp = (b[i] == 0) ? 1 : 0;
					b[i] = (b[len - 1 - i] == 0) ? 1 : 0;
					b[len - 1 - i] = tmp;
				}
				if(len % 2 == 1)
					b[len / 2] = (b[len / 2] == 0) ? 1 : 0;
			}

			return A;*/
	return A
}
